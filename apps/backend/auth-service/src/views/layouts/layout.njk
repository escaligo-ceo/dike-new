<!DOCTYPE html>
<html lang="it">
<head>
	<meta charset="UTF-8">
	<title>Admin - Auth Service</title>
</head>
<body>
<div class="admin-layout">
	<aside class="admin-sidebar">
		<div class="admin-sidebar-title">Auth Admin</div>
		<nav class="admin-sidebar-menu">
			<ul>
				<li><a href="/admin/users" class="{% if active == 'users' %}active{% endif %}">Gestione utenti</a></li>
				<li><a href="/admin/watched-person" class="{% if active == 'watched' %}active{% endif %}">Persone sotto osservazione</a></li>
			</ul>
		</nav>
	</aside>
	<main class="admin-main-content">
		{% block content %}{% endblock %}
	</main>
</div>


<style>
.admin-layout {
  display: flex;
  min-height: 100vh;
  background: #f5f6fa;
}
.admin-sidebar {
  width: 240px;
  background: #232946;
  color: #fff;
  padding: 2rem 1rem 2rem 2rem;
  display: flex;
  flex-direction: column;
  gap: 2rem;
}
.admin-sidebar-title {
  font-size: 1.5rem;
  font-weight: 700;
  margin-bottom: 1rem;
  letter-spacing: 1px;
}
.admin-sidebar-menu ul {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
.admin-sidebar-menu a {
  color: #fff;
  text-decoration: none;
  font-size: 1.08rem;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  transition: background 0.15s;
  display: block;
}
.admin-sidebar-menu a.active, .admin-sidebar-menu a:hover {
  background: #394867;
}
.admin-main-content {
  flex: 1;
  padding: 2rem 2.5rem;
  background: #f5f6fa;
}

<style>
.admin-users-panel {
  max-width: 100vw;
  padding: 2rem;
}
.admin-users-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.5rem;
}
.admin-users-title {
  font-size: 2rem;
  font-weight: 600;
  margin: 0;
}
.admin-users-search input[type="search"] {
  padding: 0.5rem 1rem;
  border-radius: 4px;
  border: 1px solid #ccc;
  margin-right: 0.5rem;
}
.admin-users-search button {
  padding: 0.5rem 1.2rem;
  border-radius: 4px;
  border: none;
  background: #2563eb;
  color: #fff;
  font-weight: 500;
  cursor: pointer;
}
.admin-users-table-wrapper {
  overflow-x: auto;
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.04);
}
.admin-users-table {
  width: 100%;
  border-collapse: collapse;
  min-width: 900px;
}
.admin-users-table th, .admin-users-table td {
  padding: 0.75rem 1rem;
  text-align: left;
  border-bottom: 1px solid #f0f0f0;
}
.admin-users-table th {
  background: #f9fafb;
  font-size: 1rem;
  font-weight: 600;
  color: #222;
}
.admin-users-usercell {
  display: flex;
  align-items: center;
  gap: 0.75rem;
}
.admin-users-avatar {
  width: 32px;
  height: 32px;
  background: #e0e7ef;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.2rem;
}
.admin-users-userinfo {
  display: flex;
  flex-direction: column;
}
.admin-users-username {
  font-weight: 500;
  color: #222;
}
.admin-users-email {
  font-size: 0.95em;
  color: #666;
}
.admin-users-badge {
  display: inline-block;
  padding: 0.2em 0.7em;
  border-radius: 12px;
  font-size: 0.95em;
  font-weight: 500;
}
.admin-users-badge--success {
  background: #e6f9e6;
  color: #1a7f37;
}
.admin-users-badge--danger {
  background: #ffeaea;
  color: #c81e1e;
}
.admin-users-actions {
  display: flex;
  gap: 0.5rem;
}
.admin-users-action-btn {
  background: none;
  border: none;
  font-size: 1.1rem;
  cursor: pointer;
  padding: 0.3em 0.5em;
  border-radius: 4px;
  transition: background 0.15s;
}
.admin-users-action-btn--danger {
  color: #c81e1e;
}
.admin-users-action-btn--danger:hover {
  background: #ffeaea;
}
.admin-users-action-btn:hover {
  background: #f0f0f0;
}
.admin-users-empty {
  text-align: center;
  color: #888;
  font-style: italic;
}
@media (max-width: 900px) {
  .admin-users-table {
    min-width: 600px;
  }
}
</style>
</body>
</html>
